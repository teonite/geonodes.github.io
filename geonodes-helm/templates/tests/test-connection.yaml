apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geonodes-helm.fullname" . }}-test-connection"
  labels:
    {{- include "geonodes-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "geonodes-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
